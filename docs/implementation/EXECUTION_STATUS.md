# NeoArc Agentic Experience Kit — Execution Status

This file is the source of truth for build lifecycle state. It is updated as implementation progresses so repository state — not chat history — records what has actually been built and gated.

```text
Slice 1  COMPLETE
Gate 1   PASS (full — hardening pass closed the earlier lint deviation)

Slice 2  COMPLETE
Gate 2   PASS

Slice 3  COMPLETE
Gate 3   PASS

Slice 4  COMPLETE
Gate 4   PASS

Slice 5  READY / NOT STARTED
Gate 5   NOT EVALUATED

Slice 6  BLOCKED BY GATE 5
Gate 6   NOT EVALUATED

Slice 7  BLOCKED BY GATE 6
Gate 7   NOT EVALUATED

Slice 8  BLOCKED BY GATE 7
Gate 8   NOT EVALUATED
```

## Log

- Master plan approved with two architectural corrections (token ownership, primitive ownership). See `MASTER_IMPLEMENTATION_PLAN.md`.
- Slice 1 implementation started.
- Slice 1 implementation complete: `src/neoarc-agentic-contracts`, `src/neoarc-agentic-projection`, `src/neoarc-agentic-ui` (17 foundation components + primitives + kit-owned tokens/themes), showcase theme wiring, and the Execution Lab route (`app/execution-lab/page.tsx`) with scenario selector, 4 observability tabs, raw-event/projected-node inspectors, live event log, and registry status footer.
- Verified in browser: light/dark theme toggle, node selection populating both inspectors, `inspector.node.select` UI event appearing in the event log, registry status footer reflecting live registrations.
- `npx tsc --noEmit` and `npx next build` both pass cleanly; both routes (`/`, `/execution-lab`) prerender as static content.
- Gate 1 evaluated against `11_V0_GATE_CHECKLIST.md`: all items pass except lint, which is not evaluable — this scaffold ships no ESLint config (`eslint.config.*` absent) and none was added, since introducing a new lint setup is a tooling decision outside Slice 1's scope and was not requested. Flagging per standing instruction rather than silently adding a dependency; typecheck + build are the enforced quality gates for this slice.
- Docs written: `COMPONENT_CATALOG.md`, `DATA_MODEL.md`, `EVENT_MODEL.md`, `PROJECTION_MODEL.md`, `RENDERER_REGISTRY.md`, `SURFACE_REGISTRY.md`, `INTEGRATION_GUIDE.md`, `ACCESSIBILITY.md`, `TRACEABILITY_PRINCIPLES.md`.
- **Gate 1 hardening pass** (closed the one flagged deviation plus three follow-up findings):
  - Added a real, working ESLint setup (`eslint.config.mjs`, flat config, `eslint@9` + `eslint-config-next`) — no longer just flagged and skipped. This surfaced two genuine React purity bugs, both fixed: `ThemeProvider` now synchronizes with `localStorage`/`matchMedia` via `useSyncExternalStore` instead of a two-render `useEffect(setState)` dance; `Timestamp` now reads `Date.now()` once through a lazy `useState` initializer instead of calling it directly in the render body.
  - Added a **Foundation Component Gallery** (`components/showcase/execution-lab/component-gallery/`, fixtures in `lib/showcase/gallery-fixtures.ts`) as a second Execution Lab mode alongside Scenario Replay (`LabModeSwitch`, `app/execution-lab/page.tsx`) — every one of the 17 foundation components plus the load-bearing primitives (Badge, Surface, Spinner) now has a live, prop/state-driven demo, independent of the event -> projection -> renderer path. Gallery mode keeps the semantic UI event log visible so interactive components (EntitySwitcher) prove out real `AgenticUIEvent` emission.
  - Rebuilt `EntitySwitcher` on `@base-ui/react`'s `Select` parts instead of a hand-rolled `<button>` + absolutely-positioned `<ul>` — correct opening/focus behavior, roving keyboard nav (arrows/Home/End/type-ahead), Escape/click-outside close, focus restoration, and full ARIA listbox/option semantics now come from the primitive rather than being asserted in docs without matching code. `@base-ui/react` is plain, framework-neutral React, consistent with kit portability rules. Updated `COMPONENT_CATALOG.md` and `ACCESSIBILITY.md` to describe the real implementation.
  - Normalized `RuntimeStatus`'s `"succeeded"` member to `"completed"` across `foundation.ts`, `RuntimeStatusBadge`, `lib/showcase/fixtures.ts`, and `DATA_MODEL.md`/`COMPONENT_CATALOG.md`, aligning it with the normalized runtime event vocabulary (`run.completed`, `task.completed`) documented in `EVENT_MODEL.md` and flagging that a richer `RunStatus` may still arrive in Slice 4.
  - Re-verified after the pass: `npx eslint .`, `npx tsc --noEmit`, and `npx next build` all pass cleanly; both routes still prerender as static content. Browser-verified in both light and dark themes: Component Gallery renders and scrolls correctly, `EntitySwitcher` opens via click, exposes `combobox`/`listbox`/`option` ARIA roles, supports selection, updates `activeId`, and emits a real `gallery.entity_switcher.select` `AgenticUIEvent` visible in the live event log; Scenario Replay mode continues to work unchanged after the mode-switch refactor.
- Slice 2 is unblocked pending user confirmation to proceed.
- **Slice 2 implementation complete** — conversation contracts, projection, and UI:
  - `src/neoarc-agentic-contracts`: `conversation.ts` (the ten `ConversationTimelineItem` kinds — user-message, agent-message, activity, tool, clarification, handoff, artifact, notice, error, retry — plus `ConversationMessage`, `MessageContentBlock`, `MessageAuthor`), `conversation-events.ts` (the fifteen normalized `conversation.*` backend/runtime event payloads), `conversation-ui-events.ts` (the semantic UI event payloads `AgentConversation` emits: `citation.open`, `artifact.open`, `attachment.open`, `handoff.open`, `toolActivity.toggle`, `clarification.submit`, `conversation.stop.request`, `conversation.retry.request`).
  - `src/neoarc-agentic-projection/conversation-node-definitions.ts`: the nine kit-provided `AgenticNodeDefinition`s covering all ten conversation item kinds (one definition handles all three message-lifecycle events, routing user vs. agent by `author.kind`), each keying strictly off the business id carried in its payload (`messageId`/`tool.id`/`clarification.id`/`handoffId`/`artifact.id`) for accumulating kinds, or the envelope's own `id` for one-shot kinds (notice/error/retry) — never off array position — so live append and full replay converge by construction.
  - `src/neoarc-agentic-ui/conversation/` (17 components): `AgentConversation` (the controlled root — `items` in, `onEmitEvent` out, closed ten-case dispatch owned by this one component, not a kit-wide mega-switch), `ConversationMessage`, `HumanMessage`, `AgentResponse`, `MessageContentRenderer`, `AgentComposer`, `ClarificationCard`, `ActivitySummaryList`, `ToolActivityDisclosure`, `CitationGroup`, `AttachmentList`, `ArtifactReferenceCard`, `AgentHandoffCard`, `AsyncWorkCard`, `ResponseActions`, `ConversationEmptyState`, `GenericAgenticNodeFallback`. All fully controlled, typed props in / typed semantic events out, no networking or effects.
  - Execution Lab: real replay engine (`components/showcase/execution-lab/use-event-replay.ts` — Play/Pause/Step forward/Step back/Reset, driving `visibleEventCount` into `applyEvents`), eight scenario fixtures (`lib/showcase/conversation-fixtures.ts` — empty, ordinary exchange, streaming assistant, tool running/completed, clarification pending/resolved, retry, handoff, async work), `ConversationNodeRenderer` registered against the shared `RendererRegistry` for all nine conversation.* kinds plus the generic fallback, and dual inspectors (current-event JSON, selected-node's own source envelope resolved by business id via `find-source-event.ts` — not by a naive `event.id === node.key` comparison, since node keys are business ids and never equal event ids).
  - Deterministic tests (`npm test`, Node's built-in test runner against `.test.mts` files, 15 tests / 15 passing): `projection-store.test.mts` proves the replayability invariant generically (full replay === live append, stable-key updates never reorder, unmatched events no-op, no mutation, resuming from an intermediate store converges); `conversation-node-definitions.test.mts` proves the same convergence property against every Execution Lab fixture scenario plus per-kind stable-identity assertions (streaming deltas merge into one node; tool/clarification/handoff update in place across their lifecycle; one-shot vs. business-id keying is exactly as documented).
  - Fixed during hardening: a literal escaped-unicode string rendering as raw text instead of a curly quote in the Inspectors panel description; a `set-state-in-effect` lint violation from a redundant `useEffect` duplicating what the scenario-select handler already did (removed, and made the handler explicitly call `replay.reset()` since two scenarios can share an event count); and the real functional bug the "selected node's source envelope" tests above guard against, caught by manual browser verification before being formalized as tests.
  - Verified in browser (light + dark) with `agent-browser`: scenario switching, Play/Step/Reset controls, streaming-delta merge rendering live, citation rendering, node selection populating the correct (previously incorrect) source-envelope panel for message, tool, clarification, and handoff node kinds, and the Component Gallery tab continuing to work unchanged.
  - Docs updated: `PROJECTION_MODEL.md` (`Replayability invariant` section now points at the real Slice 2 test files instead of describing them as future work).
  - `npx tsc --noEmit`, `npx eslint . --max-warnings=0`, and `npm test` all pass cleanly.
  - **Gate 2 evaluated against `11_V0_GATE_CHECKLIST.md`: PASS, no exceptions.** Conversation is controlled (`AgentConversation`'s `items`/`onEmitEvent` contract); the renderer registry is actually used (`ConversationNodeRenderer` resolved through `RendererRegistry`, not a bespoke switch in the Execution Lab); no central mega-switch spans unrelated node families (`AgentConversation`'s ten-case dispatch is closed and self-contained, per its own doc comment); `GenericAgenticNodeFallback` is registered as the fallback; stable node keys survive updates (proven by the new tests, not just asserted); streaming updates use `animation-frame` publication cadence and were confirmed visually smooth in the browser; no `fetch`/`axios`/`useEffect` exists anywhere in `src/neoarc-agentic-contracts`, `src/neoarc-agentic-projection`, or `src/neoarc-agentic-ui`; replay controls (Play/Pause/Step/Reset) work, browser-verified; full replay and incremental append converge in tests (15/15 passing) as well as in fixtures (the Execution Lab drives both paths through the identical `applyEvents`/`applyEvent` reducer); no hidden chain-of-thought appears anywhere in the conversation contracts, UI, or fixtures (only doc comments explicitly guarding against it).
- **Slice 2 documentation closed out**: `COMPONENT_CATALOG.md` expanded to document the full Conversation family (all 17 components, their props, emitted events, and states); `DATA_MODEL.md` gained a "Conversation models (`conversation.ts`, Slice 2)" section tabulating every Slice 2 contract type and calling out the direct-vs-projected dual-use of `ConversationTimelineItem`; `INTEGRATION_GUIDE.md` gained a "Slice 2 worked example" section walking both integration paths side by side — Path B via the Execution Lab's Scenario Replay tab (`conversation-fixtures.ts` → `useEventReplay` → `conversationNodeDefinitions` → `executionLabRendererRegistry` → `ConversationNodeRenderer`) and Path A via the Component Gallery's conversation section (`conversation-gallery-fixtures.ts` feeding `AgentConversation` directly, no event stream or projector involved) — and corrected a stale claim that Slice 1's showcase-only projector was still the only worked example.
- **Final Slice 2 verification pass**: re-ran `npx tsc --noEmit` (clean), `npx eslint . --max-warnings=0` (clean), `npm test` (15/15 passing), and `npx next build` (clean production build, both `/` and `/execution-lab` prerender as static content). Browser-verified in both light and dark themes with `agent-browser`: theme toggle switches correctly and persists via `localStorage`; the "Ordinary exchange" scenario replays end-to-end (Play advances 1/4 → 4/4), rendering the human message and the agent's cited reply with correct kind badges, avatars, and timestamps in both themes; the Component Gallery's direct-view-model conversation thread continues to render unchanged.
- Slice 3 is unblocked pending user confirmation to proceed.
- **Gate 3 review fix** (execution-permission state model): `ExecutionPermissionRequest` in `human-interaction.ts` is now a discriminated union on `status` (`pending` | `submitted` | `resolved`), so `resolved` without an `outcome` and a bare `status: "cancelled"` are both unrepresentable — cancellation is modeled only as `status: "resolved"`, `outcome: "cancelled"`. `resolveExecutionPermissionCardMode` now exposes four genuine, exhaustively-switched render modes (`pending`, `submitted`, `resolved`, `blocked`) instead of collapsing `pending`/`submitted` into one; `ExecutionPermissionCard` renders enabled buttons in `pending`, disabled buttons + "Awaiting confirmation…" in `submitted`, `PermissionOutcomeBadge` only in `resolved`, and `PermissionBlockedState` only in `blocked`. Replaced the invalid cross-product test (which exercised an illegal `status: "cancelled"` bag) with four focused tests against literal, legal `ExecutionPermissionRequest` fixtures, plus a compile-time-only `human-interaction.type-invariants.ts` (`tsc --noEmit` only, not `npm test`) asserting via `@ts-expect-error` that resolved-without-outcome, cancelled-as-status, and pending-with-outcome all fail to typecheck. Re-verified: `npx tsc --noEmit` clean, `npx eslint .` clean, `npm test` 23/23 passing, `npx next build` clean, and browser-verified in the Component Gallery that all four `ExecutionPermissionCard` states (pending, submitted, resolved, blocked/unavailable) render correctly in dark mode.
- **Gate 3 evaluated against `11_V0_GATE_CHECKLIST.md`: PASS.** Clarification (`ClarificationCard`, Slice 2) and execution permission (`ExecutionPermissionCard`/`ExecutionPermissionDialog`) exist; business proposal decisions exist separately (`ProposalSummary`/`DecisionBar`/`proposal.ts`) and are never rendered as if they answer the execution-permission question (see the module doc comments in `human-interaction.ts` and `proposal.ts`); permission and proposal are not a shared enum — `ExecutionPermissionOutcome` and `DecisionAction` are distinct types with no overlap; the UI only shows an authoritative outcome once `status === "resolved"` (now compiler-enforced) and never infers it from a click; `PendingInteraction`/`PendingInteractionsList` summarize outstanding human interactions; stale (`galleryStaleProposal`), action-failed (`lastActionFailed`), and permission-blocked (`PermissionBlockedState`) states all exist and are exercised in the Component Gallery; semantic UI event payloads (`permission.allowOnce.request`, `permission.reject.request`, `permission.cancel.request`, proposal decision events) are visible in the Execution Lab's live event log.
- Slice 4 is unblocked pending user confirmation to proceed.
- **Slice 4 scope note (explicit, user-directed):** the user approved a budget-optimized scope for this pass covering only the high-value visual/runtime work, and explicitly deferred `STATE_MODEL.md`, `RUNTIME_MODEL.md`, `docs/wiring/runtime/`, exhaustive Gate 4 prose, and a full 12-scenario Execution Lab sweep to a separate human-review pass outside v0. This entry documents what shipped and what was deliberately left out — it is not a Gate 4 evaluation.
- **Slice 4 runtime contracts/UI (built earlier this slice, prior to this pass):** `src/neoarc-agentic-contracts/runtime.ts` (`MissionSummary`, `RunSummary`/`RunStatus`, `AgentTask`, `RunError`, `WorkflowGroup`, `ExecutionStep`), `runtime-events.ts` (normalized `mission.*`/`run.*`/`task.*`/`workflow.*` backend event payloads), `runtime-ui-events.ts` (semantic UI events: `run.cancel.request`, `run.retry.request`, `run.resume.request`, `run.output.open`, `run.task.open`, `run.humanAction.open`). `src/neoarc-agentic-projection/runtime-node-definitions.ts` — the `mission.mission` / `mission.run` / `mission.task` `AgenticNodeDefinition`s, keyed by business id (`missionId`/`runId`/`taskId`), producing a `RunProjection` (run + derived pending interaction/error/outputs) for `mission.run` rather than a raw event echo. `src/neoarc-agentic-ui/runtime/` (11 components): `MissionHeader`, `RunStatusBadge`, `RunStatusPanel`, `RunActions` (status-gated: never renders both cancel and resume), `WaitingForHumanBanner` (reuses `PendingInteraction` from Slice 3, does not duplicate its shape), `RunErrorPanel`, `RunOutputs`, `AgentTaskRow`, `AgentTaskInspector`, `WorkflowRunTree` (phase/task tree; a running/failed/waiting phase cannot collapse — enforced by `workflow-tree-logic.ts` and covered by `workflow-tree-logic.test.mts`), `ExecutionTimeline` (flat chronological sibling view of the same facts). All controlled, typed props in / typed `RuntimeUIEventPayload` events out, no networking or effects.
- **Slice 4 visual core wiring (this pass):**
  - `components/showcase/execution-lab/runtime-node-renderer.tsx` — the renderer registered against every `mission.*` (target, kind) pair, composing `MissionHeader` for `mission.mission` and `RunStatusPanel`/`RunActions`/`WaitingForHumanBanner`/`RunErrorPanel`/`RunOutputs` for `mission.run`, `AgentTaskRow` for `mission.task`. Registered in `lib/showcase/registry-bootstrap.ts` by iterating `runtimeNodeDefinitions` (not hardcoded per-kind), so a new runtime node definition is picked up automatically.
  - A new "Mission" tab added to `executionLabTabs` (`lab-tab-bar.tsx`) and a `"runtime"` scenario family branch in `render-canvas.tsx` (selects `runtimeNodeDefinitions` for projection, wires `onEmitRuntimeEvent` alongside the existing `onEmitConversationEvent`) — additive changes, the existing foundation/conversation families are untouched.
  - `lib/showcase/runtime-fixtures.ts` — six integrated scenarios (not the full 12), each a self-contained event sequence: (1) queued → running multi-phase workflow with two agents/three tasks, (2) task handoff between agents mid-run, (3) waiting-for-human (execution-permission) mid-run, (4) recoverable failure → retry → recovery, (5) cancel-requested → cancelled (cancellation modeled as pending, not instant, matching `RunSummary.cancellation`), (6) completed with two produced outputs. Wired into `lib/showcase/all-scenarios.ts` as a third scenario family alongside foundation/conversation.
  - `lib/showcase/runtime-gallery-fixtures.ts` + new Component Gallery entries (`component-gallery.tsx`) for the reusable runtime primitives that benefit from isolated visual inspection, independent of the event → projection → renderer path: `MissionHeader`; `RunStatusBadge` (all 8 states); `RunStatusPanel`/`RunActions` (six run-status variants: running/cancellable, cancellation-requested, paused/resumable, failed-retryable, failed-not-retryable with reason and no button, completed with no actions); `WaitingForHumanBanner`/`RunErrorPanel`/`RunOutputs` (including outputs' explicit empty state); `AgentTaskRow`/`AgentTaskInspector` (including the inspector's "none supplied" empty state and no-task-selected state); `WorkflowRunTree`/`ExecutionTimeline` (including both components' empty states). `WorkflowRunTree`/`ExecutionTimeline` are demonstrated only in the gallery, not in the Mission tab's flat render canvas — the canvas renders one `AgenticViewNode` at a time and doesn't have the sibling-task lookup either tree component needs; documented in `runtime-node-renderer.tsx`'s module comment rather than silently omitted.
  - Deliberately deferred out of this pass (explicit user direction, not an oversight): `STATE_MODEL.md`, `RUNTIME_MODEL.md`, `docs/wiring/runtime/*`, a full `COMPONENT_CATALOG.md` rewrite for the runtime family, the remaining ~6 of the original 12 planned scenarios (e.g. dedicated correlation/replay-focused cases beyond the six above), and formal Gate 4 prose against `11_V0_GATE_CHECKLIST.md`. No new automated tests were added for the wiring itself (composition-only; the underlying logic — `workflow-tree-logic.ts`, projection convergence — already has coverage); adding scenario-level Execution Lab tests is left for the deferred documentation/close-out pass.
  - Verified this pass: `npx tsc --noEmit` clean, `npx eslint . --max-warnings=0` clean, `npm test` 32/32 passing (unchanged suite, no regressions), `npx next build` clean production build. Browser-verified with `agent-browser` in both light and dark themes across all six Mission-tab scenarios: multi-phase running state, agent handoff, waiting-for-human banner with a working "Review" action, retryable failure with a working "Retry" action, cancel-requested pending state followed by the terminal cancelled state (no actions render), and completed state with rendered output artifacts. Confirmed each of the five semantic runtime UI events (`run.retry.request`, `run.cancel.request`, `run.resume.request`, `run.output.open`, `run.task.open`) appears correctly in the live event log when triggered from the Component Gallery's `RunErrorPanel`/`RunActions`/`RunOutputs`/`AgentTaskRow` demos, and that `run.humanAction.open` fires correctly from the Mission tab's `WaitingForHumanBanner`.
- Slice 5 not started; blocked pending human review of this Slice 4 visual core pass.
- **Gate 4 runtime-projection hardening (2026-08-18):** removed fabricated `RunSummary`/`AgentTask` fallbacks from `runtime-node-definitions.ts`. A later `run.*` without a prior `run.started` for that `runId`, or a later `task.progress`/`task.completed` without a prior `task.started` for that `taskId`, now throws `ProjectionInvariantError` (`projection-invariant.ts` + `requireExistingNode`) instead of inventing `Run <id>` labels, `Task <id>` titles, `running` status, cancellation state, or an empty workflow. Added `runtime-node-definitions.test.mts`: all six Slice 4 Execution Lab scenarios converge full-replay vs live-append; run identity is stable through cancel lifecycle; task identity is stable through progress/completion; out-of-order run/task updates fail explicitly; projected labels/titles match `*.started` payloads only. `AgenticNodeDefinition` / `projection-store.ts` unchanged. UX, Execution Lab scenarios, and component structure unchanged. Slice 5 not started.
  - **Gate 4 evaluated against `11_V0_GATE_CHECKLIST.md`: PASS.** Missions/runs/tasks are distinct (`mission.mission` / `mission.run` / `mission.task` with `MissionHeader` / `RunStatusPanel` / `AgentTaskRow`); hierarchical workflow exists (`WorkflowRunTree` + `workflow-tree-logic.ts`; gallery-only on the Mission tab, as previously documented); active/error/cancelled branches stay force-expanded (`isForceExpandedStatus`); waiting-for-human is prominent (`WaitingForHumanBanner` + `runtime-waiting-for-human`); `AgentTask` carries supplied `inputRefs`/`knowledgeRefs`/`relationshipRefs`/`toolCallRefs`/`outputRefs` and the inspector renders missing lists as "None supplied"; the projector no longer invents runtime facts; retry/cancel/error states exist (`RunActions` / `RunErrorPanel` + failure/cancel scenarios); generic contracts stay `Run`/`Mission`/`Task`/`WorkflowGroup` with no Temporal/Cadence/Activity leak.
  - Still deliberately deferred (not generated in this pass): `STATE_MODEL.md`, `RUNTIME_MODEL.md`, `docs/wiring/runtime/*`, a full `COMPONENT_CATALOG.md` rewrite for the runtime family, and the remaining ~6 of the original 12 Execution Lab scenarios.
  - Verified this pass (pnpm): `pnpm exec tsc --noEmit` clean, `pnpm exec eslint . --max-warnings=0` clean, `pnpm test` 38/38 passing, `pnpm exec next build` clean production build (`/` and `/execution-lab` prerender as static content).
- Slice 5 remains READY / NOT STARTED. Do not begin it until explicitly requested.

